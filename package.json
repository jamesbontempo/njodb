{
  "name": "njodb",
  "version": "0.4.44",
  "description": "A persistent, partitioned, concurrency-controlled, Node.js JSON object database",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "mocha",
    "coverage": "nyc npm run test",
    "report-coverage": "nyc report --reporter=text-lcov > coverage.lcov && codecovcli --codecov-yml-path codecov.yml upload-process -r jamesbontempo/njodb --sha $(git rev-parse HEAD) --branch $(git rev-parse --abbrev-ref HEAD) -f coverage.lcov"
  },
  "types": "index.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/jamesbontempo/njodb.git"
  },
  "keywords": [
    "JSON",
    "object",
    "database",
    "nosql",
    "persistent",
    "partition",
    "concurrent",
    "db",
    "data",
    "storage"
  ],
  "author": {
    "name": "James BonTempo",
    "email": "jamesbontempo@gmail.com"
  },
  "license": "MIT",
  "devDependencies": {
    "chai": "^6.2.2",
    "eslint": "^10.9.1",
    "mocha": "^12.0.0",
    "nyc": "^18.0.0"
  },
  "dependencies": {
    "proper-lockfile": "^4.1.2"
  },
  "overrides": {
    "diff": ">=8.0.3",
    "serialize-javascript": ">=7.0.5"
  }
}
